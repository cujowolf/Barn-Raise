{
  "name": "BarnRaise",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ BarnRaise",
    "create-local": "graph create --node http://localhost:8020/ BarnRaise",
    "remove-local": "graph remove --node http://localhost:8020/ BarnRaise",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 BarnRaise"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
